<Page x:Class="Todo.MainEmpty1"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Todo"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="MainEmpty1">

    <Grid>
        <Image x:Name="Photo" HorizontalAlignment="Left" Height="70" Margin="10,10,0,0" VerticalAlignment="Top" Width="80" MouseLeftButtonDown="Photo_MouseLeftButtonDown" Source="/Снимок экрана 2025-09-19 204533.png">
            <Image.ContextMenu>
                <ContextMenu x:Name="Menu">
                    <MenuItem Header="Выход" Click="Exit_Click" />
                    <MenuItem Header="Изменить фото профиля" Click="ChangeProfilePhoto_Click"/>
                </ContextMenu>
            </Image.ContextMenu>
        </Image>
        <Label Content="Добро пожаловать!" HorizontalAlignment="Left" Margin="164,70,0,0" VerticalAlignment="Top" FontSize="36"/>
        <Label Content="Создайте свою первую&#xD;&#xA;задачу!" HorizontalAlignment="Left" Margin="164,168,0,0" VerticalAlignment="Top" FontSize="36" Width="529"/>
        <Button Content="Создать первую задачу" HorizontalAlignment="Center" Margin="0,344,0,0" VerticalAlignment="Top"  FontSize="20" Click="Button_Click"  Height="41" Width="244" BorderBrush="#FFEA5656">
            <Button.Style>
                <Style TargetType="Button">
                    <Setter Property="Foreground" Value="#FFFEFEFE"/>
                    <Setter Property="Background" Value="#FFEA5656"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="Button">
                                <Border x:Name="border"
Background="{TemplateBinding Background}"
BorderBrush="{TemplateBinding BorderBrush}"
BorderThickness="{TemplateBinding BorderThickness}"
CornerRadius="15,15,15,15">
                                    <ContentPresenter HorizontalAlignment="Center" 
            VerticalAlignment="Center"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="border" Property="Background" Value="#FFFEFEFE"/>
                                        <Setter Property="BorderBrush" Value="#FFEA5656"/>
                                        <Setter Property="Foreground" Value="#FFEA5656"/>

                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </Button.Style>
        </Button>

    </Grid>
</Page>
